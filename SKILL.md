---
name: direct-citation-assistant
description: 不依赖 Zotero 软件、直接管理论文引用的完整工作流。当用户要求“给论文加引用”“这句需要引文献”“帮我逐句检查是否需要引用”“整理参考文献目录”“在 Word 里插入带超链接的引用”“论文引用不要用 Zotero”“把论文下载下来按题目重命名”“引用数字自动重排”等，或需要按主题（期刊论文/毕业论文-开题报告、中期报告/课程论文）完成从确定工作文件夹→逐句判断→检索筛选文献（中英文）→收集引用信息→插入可点击跳转的超链接引用→按目标期刊/学校格式生成参考文献目录的流程时使用。核心能力：自动创建工作文件夹（三个子文件夹）；正文占位符 [?]/[CITE:key] 自动分配编号并生成可点击跳转的超链接引用；支持编号制与作者-年份制；删除中间引用后重跑脚本自动重编号；文末自动生成/更新参考文献目录；引用信息与论文 PDF 统一归档管理。
---

# 直接论文引用助手（不依赖 Zotero）

把一个写作任务变成带规范引用的 Word 文稿。整体流程：

0. **定工作文件夹**：确认项目文件夹；未指定则自动在 C 盘外创建（三个子文件夹）
1. **定主题与期刊**：期刊论文（哪本刊）／毕业论文（开题、中期）／课程论文 → 搜索该目标引用格式
2. **定粒度**：这句要引 / 这段要引 / 整篇准备引用
3. **逐句检索**：一句一句判断是否需要引用；需要则检索文献（强支持观点 > 最新 > 高评分；毕业论文注意中文文献）
4. **收集引用信息**：从 Google Scholar / 知网等复制题录 → `add_refs.py` 写入 `引用目录/refs.csv`；下载论文 PDF → `rename_papers.py` 按题目归档到 `文献/`
5. **插入引用**：正文放占位符 `[?]`/`[CITE:key]` → 按期刊格式运行 `insert_refs.py` 生成可点击跳转的超链接引用 + 文末参考文献表；删掉中间引用后重跑即自动重编号

## 步骤 0：确定/创建工作文件夹（动手前必做）

- **用户已指定文件夹** → 直接用，建议内部按下面三子文件夹组织：
  ```
  <项目文件夹>/
  ├── 论文/          # 存放 Word 论文文稿
  ├── 引用目录/       # 存放引用信息表 refs.csv（引用目录列表）
  └── 文献/          # 存放对应的论文 PDF（能下载的尽量下载）
  ```
- **用户未指定** → 运行 `scripts/new_project.py` 自动创建：
  ```bash
  python "<SKILL目录>/scripts/new_project.py" --name "项目名" [--base "E:\文件1号"]
  ```
  脚本自动在 **C 盘之外**（按 D、E、F… 找第一个可写盘；找不到退回用户主目录）创建项目文件夹及三个子文件夹，并生成带表头的 `引用目录/refs.csv` 模板。`--name` 建议「主题_阶段」（如 `毕业论文_开题报告`）。
- 项目名称缺省用日期；目标文件夹已存在且非空时会拒绝创建，避免覆盖。

## 步骤 1：确定主题、期刊与格式（决定编号与目录样式）

- 先问清/判断目标：**期刊论文**（确定是哪本刊！）／**毕业论文**（开题报告、中期报告，按学校模板）／**课程论文**（看老师要求）。
- **必须用 `general_search` 搜索该目标的引用格式要求**：
  - 期刊 → `<期刊名> 参考文献格式` / `<journal name> reference style` / `<journal> author guidelines references`；
  - 学校毕业论文 → `<学校名> 毕业论文 参考文献格式`；课程论文 → 问老师或按学校通用要求。
- 搜到的格式要点决定 `insert_refs.py` 的参数组合：
  - 常见格式体系：**GB/T 7714**（中文期刊/中文毕业论文最常用）→ `--style gbt7714 --citation numbered`；
    **Vancouver/ICMJE**（生物医学期刊）→ `--style vancouver --citation numbered`；
    **APA** → `--style apa --citation author-year`；**MLA** → `--style mla --citation author-year`。
  - 编号/引用样式：编号制（`--citation numbered`，正文 [1] 上标，`--bracket []` 或 `()`、`--no-superscript` 关闭上标）或作者-年份制（`--citation author-year`，正文 (Smith et al., 2023)）。
  - 文末条目编号：`--entry-num bracket`（[1]）或 `num`（1.）。
  - 记录格式要点清单（引用顺序、作者截断、期刊名斜体/缩写、卷期页、DOI、标点、中英文），便于核对生成结果。

## 步骤 2：确定引用粒度

- **单句**：只处理用户点名的句子。
- **段落**：整段逐句检查，把需要引用的句子标出来。
- **整篇**：全文逐句过一遍，需要引用的句子都做上标记；引用信息统一整理成表格（`引用目录/refs.csv`，Excel 可直接打开），下载的论文按题目重命名归档（见步骤 4）。

## 步骤 3：逐句判断 + 检索筛选（含中文文献）

### 3a. 引用必要性判断（先分辨“这句要不要引用”）

- **需要引用**：具体事实/数据/统计陈述、他人（前人）的发现与结论、方法或技术的出处、术语/概念的定义与首创、与领域既有结论的对照或争议陈述、支撑本文观点成立的关键前提。
- **不需要引用**：作者自己的论点、推理与总结（“我们提出/我们认为”）、前文承接与过渡、领域公认常识性背景、本文工作自述（“In this study, we …”、“图 X 显示”）、研究意义与展望。
- 拿不准时**从严**：只给真正需要支撑的关键论断配文献，同一论断 1~3 篇即可，不为凑数堆引用。

### 3b. 检索与选文三原则（按优先级）

1. **证据强度优先**：选能**强烈证明该句观点**的文献——原始出处/直接做该结论的研究/方法原文/权威综述；排除只沾边、间接、弱相关的。
2. **时效性次之（越新越好）**：证据强度相当时优先最新年份；最强证据是经典文献时，用“经典原始文献 + 近年最新综述”组合。
3. **评分/引用量参考**：同强度同年代候选中，优先高被引/高评分（Google Scholar 引用次数、期刊影响因子/分区参考，不绝对化）。

- 解析句子：提取核心论断、方法名/术语、作者-年份线索。句子提及著名论文或“xx et al. (年)”→ 直接锁定该文。
- **检索渠道（完整清单见 `references/websites.md`）**：
  - 英文/国际文献：`scholar_search` / `general_search` / Google Scholar（中英文关键词都试）；
  - **中文文献（毕业论文、中文期刊必查）**：知网 CNKI、万方、维普、百度学术、Google Scholar 中文页面；关键词用中文术语 + 可能的英文对应词交叉检索。中文综述类文献常能一句话覆盖多个背景点，优先考虑。
  - 按领域补充：生物医学用 PubMed；计算机/物理可查 arXiv、IEEE Xplore；DOI 核验用 Crossref；引用关系可用 Semantic Scholar / OpenAlex；权威引文索引 Web of Science / Scopus（订阅）。
  - 检索前先看 `references/websites.md` 中对应网站的状态标记（✅ 可达 / ⚠️ 反爬需浏览器 / ❌ 不可达），优先选用可达渠道。
- **毕业论文场景**：目标学校/专业若要求或偏好中文文献（尤其中文背景、国内研究现状部分），主动检索中文文献并纳入；refs.csv 直接支持中文（UTF-8），GB/T 7714 会自动按中文作者与中文标点排版。
- 每篇记录：标题、作者、期刊/来源、年份、卷期页、DOI、来源 URL、选择理由。

## 步骤 4：收集引用信息 + 论文归档（工作区三子文件夹）

- **引用信息表**：`<项目>/引用目录/refs.csv`（UTF-8 with BOM，Excel 可直接打开），字段：
  `key, type, title, authors, source, year, volume, issue, pages, city, doi, url, note`
  - `city`（出版地/保存地）：专著 [M] / 学位论文 [D] / 会议录 [C] 的 GB/T 7714 格式需要，如学位论文填保存地"北京"、专著填出版地"北京"；
  - `url`：网页 [EB/OL] 类型必须填（GB 格式自动输出 `(年)[引用日期]. URL`，引用日期自动取当天）。
- **录入规范**（避免格式细节错误）：APA 的题名请按句首大写（sentence case）录入；MLA/APA 的 DOI 脚本会自动补 `https://doi.org/` 前缀；期刊/会议英文题名首字母大小写按原文录入。
- 在 Google Scholar/知网搜索论文 → 复制题录（作者/标题/期刊/年/卷/期/页/DOI）→ 添加：
  ```bash
  python "<SKILL目录>/scripts/add_refs.py" --refs <项目>/引用目录/refs.csv ^
      --title "题名" --authors "Smith, J., Jones, K." --source "期刊名" ^
      --year 2023 --volume 10 --issue 2 --pages "100-110" --doi "10.xxxx/xxxx" --type journal
  ```
  或交互式：`python add_refs.py --refs <项目>/引用目录/refs.csv --interactive`；查看：`--list`。
  中文文献同样录入（作者直接写中文名，type 用 journal/thesis/book 等；学位论文用 `--type thesis`）。
- **论文下载与重命名**：在浏览器下载 PDF（能下载的尽量下载；OA 论文可直链，订阅期刊需登录）到 `<项目>/文献/` 下临时位置，然后：
  ```bash
  python rename_papers.py --refs <项目>/引用目录/refs.csv --in <项目>/文献/下载暂存 --out <项目>/文献
  ```
  自动按题名匹配重命名归档（先按文件名，再读 PDF 首页文本匹配）；匹配不上的用 `--by-key <key> --file <路径>` 手动指定。

## 步骤 5：在 Word 中插入引用（超链接 + 自动重编号 + 参考文献表）

### 5a. 在正文放置引用点（文稿放在 `<项目>/论文/`）

- `[?]` —— 按出现顺序自动取 `refs.csv` 中尚未引用的下一条。
- `[CITE:key]` —— 指定引用某条（key 见 refs.csv）。
- 上次生成的引用超链接会自动保留并重新编号/更新，无需处理。

### 5b. 按期刊格式运行插入脚本

```bash
python "<SKILL目录>/scripts/insert_refs.py" --docx <项目>/论文/文稿.docx ^
    --refs <项目>/引用目录/refs.csv --style gbt7714 --citation numbered
```

效果：
- 正文引用点变成可点击的编号/作者-年份引用（默认上标 `[1]`），**点击即可跳转到文末对应文献条目**（Word 默认 Ctrl+点击；要单击跳转：文件→选项→高级→取消勾选“用 Ctrl+单击跟踪超链接”）。
- 文末自动生成参考文献目录：找到 References/参考文献 标题则在其后更新，找不到则自动创建。
- **字体自动设置**：生成的正文引用与参考文献条目按中文论文通行规范设置字体——**中文（汉字）用宋体、英文与数字用 Times New Roman（新罗马）、西文标点随西文字体**。脚本在 run 的 rFonts 中分别指定（ascii/hAnsi=Times New Roman、eastAsia=宋体），无需手工调整；个别期刊/学校模板字体不同时用 `--font-en <西文字体>` / `--font-cn <中文字体>` 覆盖。
- **版式说明（国标不管版式）**：GB/T 7714-2015 只规范著录内容（著录项目/顺序/符号/方法/正文标注法），**不强制字体、字号、行距、缩进、对齐、编号加粗**——这些按目标期刊/学校模板。通行惯例与脚本默认：
  - 参考文献条目**悬挂缩进 2 字符**，**续行与首行内容首字符对齐**（编号后接制表符 Tab 推进；缩进用 twips 单位 w:left/w:hanging 实现，实测保证首行与正文左边缘完全对齐、续行精确缩进 2 字符；`--no-indent` 关闭）；
  - 参考文献**两端对齐**（`--align left` 改左对齐，`--align ""` 继承文档）；
  - **编号后以制表符/空格分隔内容**（山东大学等规范明确"序号后用制表符分隔"，国标示例为空格；脚本默认 Tab 精确对齐，两条目均不空行）；
  - 编号**默认不加粗**（与条目内容同格式，通行惯例）；模板要求加粗时用 `--bold-num`；
  - 条目之间**不空行**（脚本默认连续排列）；
  - **行距不预设**，随文档段落默认/学校模板，需要时在 Word 中全选参考文献段统一设置（如 1.5 倍）。
- **自动重编号**：删除中间某处引用（或改动占位符）后，重跑本脚本，其余编号自动连续更新；同一篇论文多处引用只占一个编号、文末只列一次。作者-年份制下文末按作者字母序排列、不编号。
- **正文标注位置（顺序编码制）**：引用编号用**上标**、置于方括号，**紧跟引文内容、前后不留空格**；按通行惯例放在**句末标点之前**（句号前，如「……显著进展[1]。」，[1] 在句号前、紧贴"进展"）；同一处引用多篇用 `[1,3-5]`（逗号分隔，连续用起讫号）。脚本默认上标（`--no-superscript` 改普通文字）。
- 修改前自动备份原 docx 到 `<docx同目录>/_backup/`。

常用参数：`--style gbt7714|apa|vancouver|mla`、`--citation numbered|author-year`、`--heading 自定义标题`、`--no-superscript`、`--bracket ()`、`--entry-num num`、`--font-en "Times New Roman"`（西文字体）、`--font-cn "宋体"`（中文字体）、`--no-indent`（关闭条目悬挂缩进）、`--hanging-pt 21`（悬挂缩进量 pt：五号 21 / 小四 24 / 四号 28，默认 21）、`--align both|left`（条目对齐，默认 both 两端对齐）、`--bold-num`（编号加粗，默认不加粗）、`--dry-run`（只预览）。

### 5c. 验证

- 脚本运行结束自带回读验证（引用数、书签数、编号连续性、占位符残留）。
- 用 Word 打开，点击正文引用跳转确认；按步骤 1 搜到的格式要点清单，抽查 2~3 条文末条目逐项比对（编号顺序、作者截断、期刊名、卷期页、DOI、标点、中英文）。

## 异常与注意

- **文档被 Word 占用**：先关闭 Word 再运行脚本。
- **`[?]` 多于未引用条目**：脚本报错，先 `add_refs.py` 补文献。
- **同一篇论文要引多次**：第二次引用该文献时正文放 `[CITE:同一个key]`，编号自动复用。
- **格式细节**（GB/T 7714 等）：脚本按字段自动生成；作者截断、标点等如与目标期刊/学校模板有出入，以搜索到的格式要求为准，可手工微调文末条目文本。
- **专著/学位论文/网页**：`book`/`thesis`/`conference` 类型记得填 `city`（出版地/保存地），`web` 类型必须填 `url`，否则 GB 格式会缺出版地或获取路径。
- **中文文献**：Google Scholar/知网题录直接录入即可；GB/T 7714 对中文文献（含学位论文 [D]、专著 [M]）自动处理。
- **下载论文**：部分期刊 PDF 需登录/订阅，浏览器手动下载后运行 rename_papers.py 即可。

## 脚本参考

- `new_project.py` —— 创建工作区：C 盘外自动建 论文/、引用目录/、文献/ 三子文件夹 + refs.csv 模板 + 使用说明（脚本路径动态生成）。
- `add_refs.py` —— 把 Google Scholar/知网题录写入 refs.csv（参数/交互两种模式），校验 type 合法性，写入前自动备份。
- `rename_papers.py` —— 下载的 PDF 按题名匹配重命名并归档到 文献/（文件名匹配 + PDF 内容匹配；目标重名自动加序号不覆盖）。
- `insert_refs.py` —— 核心：正文占位符→超链接引用（编号制 [n]/(n) 与作者-年份制）；文末参考文献表生成/更新；自动重编号；无 Hyperlink 样式时自动补蓝色下划线；自带备份（保留最近 30 份）与回读验证。
- `format_refs.py` —— 单条记录的格式渲染（gbt7714/apa/vancouver/mla），供 insert_refs 内部调用。
- `refs_db.py` —— refs.csv 读写工具库。
- `check_websites.py` —— 文献检索网站可用性检查（代码执行，非提示词判断）。

## 文献检索网站清单与自动检查

- 完整清单：`references/websites.md`（网站、URL、用途、状态、备注；含中英文与各领域渠道）。
- **状态由代码维护**：`scripts/check_websites.py` 对清单逐站发 HTTP 请求检查（并发、超时、确定性判定：2xx/3xx 可达、403/407 反爬拦截、4xx/5xx 异常、连接失败不可达），`--update` 把结果写回 `references/websites.md` 并记录检查时间。
- 手动运行：
  ```bash
  python "<SKILL目录>/scripts/check_websites.py" --update
  ```
- **定期自动检查**：已配置定时任务（默认每周一 10:30 运行），触发时执行上述脚本并更新清单；若发现网站状态变化（如新增不可达），在回复中简要说明并建议替代渠道。清单内容更新（增删网站）改 `check_websites.py` 内的 `SITES` 列表即可，改后重跑一次脚本。
- 状态含义：✅ 脚本直连可达；⚠️ HTTP 异常（多为反爬拦截，浏览器访问通常正常）；❌ 真正不可达（DNS/超时/连接失败），检索时优先跳过或换镜像。
